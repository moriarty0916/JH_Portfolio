{
  "name": "jh-portfolio-api",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:create": "wrangler d1 create jh-portfolio-chungli-db",
    "db:init": "wrangler d1 execute jh-portfolio-chungli-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
